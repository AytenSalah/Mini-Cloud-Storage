# Mini-Cloud-Storage
backend service in C++ that exposes HTTP APIs to upload, list, download, view metadata, and delete files. No GUI is required. Files are stored on local disk and metadata is persisted locally
